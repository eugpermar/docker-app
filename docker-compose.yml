version: '3'
volumes:
  applog:
services:
  app:
    build: .
    ports:
      - "${APP_PORT}:${APP_PORT}"
    depends_on:
      - answer_svc
    volumes:
      - applog:/logs
  answer_svc:
    build: "answer_server/"
